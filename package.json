{
  "name": "synology-drive-ignore-nm",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts --compile --outfile dist/synology-drive-ignore-nm && rm -f ./.*-*.bun-build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@loice5/dangerous-ini": "^1.0.1",
    "@types/ini": "^4.1.0"
  }
}